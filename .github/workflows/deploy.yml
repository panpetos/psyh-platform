name: 🚀 Deploy Vite front to REG.RU

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install deps
        run: npm ci || npm i

      - name: Build
        run: npm run build

      - name: Add .htaccess to dist
        run: |
          cat > dist/.htaccess << 'EOF'
          <IfModule mod_deflate.c>
            AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
          </IfModule>

          RewriteEngine On
          RewriteBase /
          RewriteCond %{REQUEST_FILENAME} !-f
          RewriteCond %{REQUEST_FILENAME} !-d
          RewriteRule . /index.html [L]

          <IfModule mod_headers.c>
            <FilesMatch "\.html$">
              Header set Cache-Control "no-cache, no-store, must-revalidate"
              Header set Pragma "no-cache"
              Header set Expires "0"
            </FilesMatch>
          </IfModule>
          EOF

      - name: FTP deploy (dist → site root)
        uses: SamKirkland/FTP-Deploy-Action@4.3.4
        with:
          server:   ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}

          # Заливаем только сборку
          local-dir: dist/

          # ВАЖНО: абсолютный путь до корня сайта вашего FTP-пользователя
          # если у пользователя root = /var/www/u3105470/data/www/psyh.vakulskiy.team/
          server-dir: /

          # Полная очистка перед загрузкой (кроме исключений)
          dangerous-clean-slate: true

          # Новый файл состояния, чтобы забыть старую структуру
          state-name: .ftp-deploy-state-front.json

          # Не трогаем api и .htaccess на сервере
          exclude: |
            api/**
            .htaccess
            **/.git*
            **/node_modules/**
            **/*.map
